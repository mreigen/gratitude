<%= form_for([@day, @grace]) do |f| %>
  <% if @grace.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@grace.errors.count, "error") %> prohibited this grace from being saved:</h2>

      <ul>
      <% @grace.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_field :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
